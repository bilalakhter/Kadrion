API:
  max_concurrent_requests: 1000
  number_of_requests: 10
  endpoints:
    - endpoint: "http://localhost:3000/health"
      method: "POST"
      JSON: >
        {
          "Health": "health",
            "Really": {
             "Check": "test"
              }
         }
